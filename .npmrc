# npm publish configuration
access=public
tag=latest

# Save exact versions
save-exact=true

# Only publish the files listed in the files field
ignore-scripts=false

# Optional: registry URL (uncomment if using a custom registry)
# registry=https://registry.npmjs.org/
